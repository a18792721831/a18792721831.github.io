---
layout: post
title: "【转载】清除线上k8s中node节点无用的镜像"
date: 2019-10-15 19:58:40 +0800
categories: [k8s集群node节点清理, k8s下docker镜像的清理, k8s释放资源, docker清理无用数据, docker 命令]
description: "本文介绍了一种在Kubernetes环境下安全地清理Docker镜像的方法，包括检查现有Pod状态、重新部署产生多余镜像、使用docker system df查看镜像占用情况以及运用docker system prune -a命令彻底清理无用镜像，确保资源有效利用。"
keywords: k8s集群node节点清理, k8s下docker镜像的清理, k8s释放资源, docker清理无用数据, docker 命令
---

> **文章信息**
> - 原文链接：https://jiayq.blog.csdn.net/article/details/102573710
> - 发布时间：2019-10-15 19:58:40
> - 阅读量：1
> - 分类：kubenetes同时被 2 个专栏收录, 订阅专栏, docker
> - 标签：#k8s集群node节点清理, #k8s下docker镜像的清理, #k8s释放资源, #docker清理无用数据, #docker 命令

## 摘要

文章浏览阅读1.4k次。本文介绍了一种在Kubernetes环境下安全地清理Docker镜像的方法，包括检查现有Pod状态、重新部署产生多余镜像、使用docker system df查看镜像占用情况以及运用docker system prune -a命令彻底清理无用镜像，确保资源有效利用。

---

转自：https://blog.51cto.com/12131824/2390176

为了以防万一（线上环境一定要谨慎谨慎再谨慎），清除镜像对已有的pod造成不必要的影响；我们以下的操作在容器最少的node节点上运行！（如果没有的话，可以重新开一台node节点）

1、先使用kubectl get po –namespace 命名空间，查看该命名空间已有的pod

2、重新部署pod，在该node节点上产生多余的images镜像

3、使用docker system df命令，在执行清除镜像之前先查看镜像和容器的数量。  
注：类似于Linux上的df命令，用于查看Docker的磁盘使用情况。这条命令可以查看到node节点中镜像和容器的数量

4、使用docker system prune –a。清除无用的镜像  
注：docker system prune命令可以用于清理磁盘，删除关闭的容器、无用的数据卷和网络，以及dangling镜像(即无tag的镜像)。docker system prune -a命令清理得更加彻底，可以将没有容器使用Docker镜像都删掉。注意，这两个命令会把你暂时关闭的容器，以及暂时没有用到的Docker镜像都删掉了…所以使用之前一定要想清楚吶。

5、使用docker system df命令，查看镜像和容器的数量；是否成功