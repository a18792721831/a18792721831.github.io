---
layout: post
title: "MySQL 索引类型"
date: 2025-08-12 16:46:44 +0800
categories: [mysql, 数据库, 索引, 索引类型, B+树]
description: "本文介绍了MySQL中的几种主要索引类型及其特点。聚集索引以B+树形式存储全部数据，通过主键聚集数据；辅助索引(二级索引)只存储索引字段和主键，查询时可能需要回表；唯一索引确保字段值唯一性；联合索引由多个字段组成，可优化多条件查询。文章通过具体SQL示例演示了各类索引的创建、使用和执行计划分析，重点说明了联合索引的最左前缀原则以及索引覆盖对查询性能的影响。合理使用这些索引可以有效提升数据库查询效率。"
keywords: mysql, 数据库, 索引, 索引类型, B+树
---

> **文章信息**
> - 原文链接：https://jiayq.blog.csdn.net/article/details/150275404
> - 发布时间：2025-08-12 16:46:44
> - 阅读量：721
> - 分类：mysql专栏收录该内容, 订阅专栏
> - 标签：#mysql, #数据库, #索引, #索引类型, #B+树

## 摘要

文章浏览阅读721次，点赞27次，收藏12次。本文介绍了MySQL中的几种主要索引类型及其特点。聚集索引以B+树形式存储全部数据，通过主键聚集数据；辅助索引(二级索引)只存储索引字段和主键，查询时可能需要回表；唯一索引确保字段值唯一性；联合索引由多个字段组成，可优化多条件查询。文章通过具体SQL示例演示了各类索引的创建、使用和执行计划分析，重点说明了联合索引的最左前缀原则以及索引覆盖对查询性能的影响。合理使用这些索引可以有效提升数据库查询效率。

---

#### MySQL 索引类型

  * 聚集索引
  * 辅助索引
  * 唯一索引
  * 联合索引

## 聚集索引

在Inno DB 中，表中的数据是以B+树的形式存储的，这种存储了所有数据的B+树一般称为聚集索引。Inno DB 通过主键聚集数据，如果没有定义主键，那么 Inno DB 会选择第一个非空的唯一索引代替，如果没有非空的唯一索引，那么Inno DB 会隐式定义一个 ROW ID 代替。

聚集索引占用的空间最大，因为保存了全部的数据。

创建一张表
    
    
    create table `t1` (
    `id` int not null auto_increment primary key,
    `idx` int not null,
    `name` varchar(20)
    ) engine = innodb default charset=utf8mb4;
    

插入一些数据：
    
    
    insert into `t1`(`idx`,`name`) values (1,'a'),(2,'b'),(3,'c'),(5,'e'),(6,'f'),(7,'g'),(9,'i');
    

查询
    
    
    select * from `t1`;
    

| id  | idx | name |
|-----|-----|------|
| 1   | 1   | a    |
| 2   | 2   | b    |
| 3   | 3   | c    |
| 4   | 5   | e    |
| 5   | 6   | f    |
| 6   | 7   | g    |
| 7   | 9   | i    |

聚集索引：

![image-20250805194813865](https://i-blog.csdnimg.cn/img_convert/0b3050528c932992f8ae1113187afe2c.png)

叶子节点上存储的是整行数据，并且叶子节点之间用双向链表关联。

## 辅助索引

辅助索引，也称为二级索引，单张表可以有多个。聚集索引的叶子节点存储的是整行数据。但是 Inno DB辅助索引的叶子节点只存放对应索引字段的键值和主键ID。

有时需要统计表的总行数，此时优化器可能会选择辅助索引作为统计目标索引，因为占用空间最小，加载也快。

在使用二级索引的时候，因为只存储了索引字段的值和主键，所以如果需要查询其他列的数据，就需要先通过二级索引中的值找到对应的主键，在通过主键找到聚簇索引中其他列的数据。–这个过程称为回表。  
为了减少回表次数，可以将语句中经常使用到的所有列以合适的顺序建立一个二级联合索引。这样所有需要的列都被这个二级索引覆盖，就不需要回表。

当通过辅助索引来检索数据时，Inno DB 先遍历辅助索引树查找对应记录的主键，然后通过主键索引找到对应的行数据。

通常采用以下几种方式创建索引
    
    
    create table `t1` (
    `id` int not null auto_increment primary key,
    `idx` int not null,
    `name` varchar(20),
    key `idx_idx` (`idx`)
    ) engine = innodb default charset=utf8mb4;
    

**在创建表的时候就创建**
    
    
    create index `idx_b` on `t1`(`idx`);
    

**通过`create index`创建**

![image-20250805194958986](https://i-blog.csdnimg.cn/img_convert/ea780d87f8b5bc80757642e4e0d1675f.png)

查看索引
    
    
    show index from `t1`;
    

![image-20250805195144077](https://i-blog.csdnimg.cn/img_convert/bedb6756c64612ed903e0c22651aa940.png)

删除索引
    
    
    drop index `idx_b` on `t1`;
    

**使用 alter table 创建索引**
    
    
    alter table `t1` add index `idx_name`(`name`);
    

删除
    
    
    alter table `t1` drop index `idx_name`;
    

## 唯一索引

唯一索引是一个不包含重复值的二级索引（可以理解为：唯一索引由唯一约束和二级索引两部分组成）。为某个字段添加唯一索引之后，那么写入该字段的值必须是不同的，否则会提示错误。

使用
    
    
    alter table `t1` add unique `t1_uniq_name`(`name`(2));
    

给name字段的前两个字符增加唯一性索引

![image-20250812161718834](https://i-blog.csdnimg.cn/img_convert/a13cbaba7aaaeeb2154e0c2a5b9bd0ed.png)

使用
    
    
    show create table `t1`;
    

查看索引

![image-20250812161823472](https://i-blog.csdnimg.cn/img_convert/7a5746cb877046f12b0daf1d35a140b4.png)

查看数据

![image-20250812161915118](https://i-blog.csdnimg.cn/img_convert/3b4be2097d4755816cb67608e6b5763d.png)

增加数据
    
    
    insert into `t1`(`idx`,`name`) values(10,'aa');
    insert into `t1`(`idx`,`name`) values(10,'aab');
    

因为已经有了`aa`，在增加 `aab`的时候报错
    
    
    Error executing INSERT statement. Duplicate entry 'aa' for key 't1.t1_uniq_name' - Connection: master: 402ms
    

![image-20250812162024124](https://i-blog.csdnimg.cn/img_convert/e9221c994781d2559625266f7c7e01b8.png)

## 联合索引

有时普通索引已经无法满足需求，如单个字段唯一性很低，需要联合多个字段才能达到最优效果，这种由多个字段组成的二级索引称为联合索引。

联合索引适用于where 条件中的多列组合，并且在某些场景中可以避免回表。

使用
    
    
    create index `idx_name` on `t1`(`idx`,`name`);
    

创建 `idx`和`name`的联合索引。

idx和name的联合索引

![image-20250805195352162](https://i-blog.csdnimg.cn/img_convert/dc4cd7e6752a8a52c819edb6e40913cc.png)

联合索引与单个键值的B+树差不多，联合索引也是按照键值排序的。

需要注意的是，当`idx`和`name`都作为条件时，查询是可以使用索引的；单独对`idx`字段进行查询也是可以使用索引的。但是单独对`name`字段进行查询就无法使用索引。

因为`name`字段对应的值`c,b,a,e,i,g,f`是无序的，所以无法使用索引。

![image-20250805195454250](https://i-blog.csdnimg.cn/img_convert/9d55b654c8942057a531d741e0801cb1.png)

可以看到 `idx_name`有两个列

查看执行计划
    
    
    explain select `name` from `t1` where `idx`=1 or `name`='c';
    

![image-20250805195836110](https://i-blog.csdnimg.cn/img_convert/fceeb473ec02abfaa1dd418d951eee09.png)

key列的值为 `idx_name` 表示使用了联合索引。key_len 列的值为87

`len(idx)=4,len(name)=20*4`，加上因为`name`允许为空，所以需要在加1.总计87

4 (int) + [2 (varchar长度前缀) + 20*4 (utf8mb4每个字符4字节) + 1 (可为NULL标记)] = 4 + (2+80+1) = 87

使用or是无法使用联合索引的，所以在index之外还要再次判断where 条件。

比如查询
    
    
    explain select `name` from `t1` where `idx`=3 and `name`='c';
    

![image-20250812163830556](https://i-blog.csdnimg.cn/img_convert/e146a9faea9f91ac76c72a3a422f566e.png)

直接使用index就能完成数据过滤。

只有`idx`作为条件时，也能使用联合索引
    
    
    explain select `name` from `t1` where `idx`=3
    

![image-20250812163936008](https://i-blog.csdnimg.cn/img_convert/a8d49d9abf681d93438407badbe722ce.png)

key列的值为`idx_name`，表示使用了索引`idx_name`。key_len列的值为4，因为 `idx`字段是不允许为空的int类型，所以key_len是4.

只有`name`作为条件时，不能使用联合索引
    
    
    explain select `name` from `t1` where `name`='c';
    

![image-20250812164126357](https://i-blog.csdnimg.cn/img_convert/6fe76a402127ecc0f544d77839d16cd9.png)

在上面的例子中，`Extra`字段的值有的是`Using index`,并且rows等于1，并且filtered=100,而且查询的字段正好是索引的字段，因此不需要回表就能获取到信息。可以减少SQL语句执行过程中的IO次数。
