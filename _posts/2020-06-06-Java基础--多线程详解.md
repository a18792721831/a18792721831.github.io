---
layout: post
title: "Java基础--多线程详解"
date: 2020-06-06 14:25:19 +0800
categories: [Java多线程, 多线程原理, 多线程常识, 多线程匪夷所思的地方, 多线程的应用]
description: "本文深入探讨Java多线程机制，覆盖进程、线程、协程概念，详细解析线程创建、线程池使用、线程状态管理、线程终止策略及线程同步机制。通过实例演示线程调度、并发控制及资源管理技巧。"
keywords: Java多线程, 多线程原理, 多线程常识, 多线程匪夷所思的地方, 多线程的应用
---

> **文章信息**
> - 原文链接：https://jiayq.blog.csdn.net/article/details/106340479
> - 发布时间：2020-06-06 14:25:19
> - 阅读量：7
> - 分类：java核心同时被 3 个专栏收录, 订阅专栏, Java8, JVM
> - 标签：#Java多线程, #多线程原理, #多线程常识, #多线程匪夷所思的地方, #多线程的应用

## 摘要

文章浏览阅读7.3k次，点赞11次，收藏54次。本文深入探讨Java多线程机制，覆盖进程、线程、协程概念，详细解析线程创建、线程池使用、线程状态管理、线程终止策略及线程同步机制。通过实例演示线程调度、并发控制及资源管理技巧。

---

#### Java基础--多线程详解

  * 1\. 进程 线程 协程
  *     * 1.1 操作系统类型
    * 1.2 线程
    * 1.3 进程
    * 1.4 线程和进程的关系
    * 1.5 协程
    * 1.6 协程和线程的关系
    * 1.7 并行&串行
    * 1.8 并发&并行
    * 1.9 阻塞&非阻塞
    * 1.10 同步&异步
  * 2\. 线程创建
  *     * 2.1 线程组成
    * 2.2 任务
    * 2.3 线程驱动任务
    * 2.4 线程这样使用的问题
  * 3\. 线程池
  *     * 3.1 Executor&ExecutorService
    * 3.2 Executors
    * 3.1 newCachedThreadPool
    * 3.2 newFixedThreadPool
    * 3.3 newScheduledThreadPool
    * 3.4 newSingleThreadExecutor
    * 3.5 ThreadPoolExecutor
  * 4\. Callable
  * 5\. 线程状态(生命周期)
  *     * 5.1 NEW
    * 5.2 RUNNABLE
    * 5.3 RUNNING
    * 5.4 BLOCKED
    * 5.5 DEAD(TERMINATED)
  * 6\. 终止线程的4种方式
  *     * 6.1 正常运行结束
    * 6.2 使用退出标志退出线程
    * 6.3 Interrupt中断线程
    *       * 6.3.1 线程阻塞
      * 6.3.2 线程不阻塞
    * 6.4 stop方法终止线程
  * 7\. sleep,wait,yield,join
  * 8\. start与run
  * 9\. daemon
  *     * 9.1 什么是daemon进程
    * 9.2 优先级
    * 9.3 daemon创建
    * 9.4 daemon传递性
    * 9.5 daemon生命周期
  * 10\. 自管理Runnable

## 1\. 进程 线程 协程

### 1.1 操作系统类型

作业是用户在一次处理中要求计算机系统做的操作的集合。  
目前操作系统基本上可以分为3类：

  1. 批处理操作系统

  * 用户提交作业给系统
  * 系统将若干作业分为一个批次加载入外存
  * 系统按一定的策略选择一个作业加载入内存执行
  * 作业运行完成或错误
  * 系统输出已执行的作业，并继续下一个作业

策略常见的有：先到先服务，优先级等。  
优点：  
减少人工干预，提交机器利用率。  
缺点：  
作业阻塞时，系统阻塞；无交互。

  2. 分时操作系统

  * 用户发出系统使用请求
  * 系统将用户请求放入时间分配范围内
  * 用户使用完成或分配用户的时间已到，暂停用户使用，分配下一用户

  3. 实时操作系统  
实时操作系统和分时操作系统相当类似，甚至其工作基本原理相似。区别在于，分时操作系统中，用户的请求可能需要很长时间才能执行完成（如果用户过多的话）；在实时操作系统中，不管用户数量是多少，操作系统必须在指定的时间内进行响应。  
如果分时操作系统的时间片分配的足够小，那么，其可以当做一个实时操作系统。

在现代操作系统中绝不是单独一个类型的操作系统，都是多种模式配合使用。

### 1.2 线程

线程是操作系统调度的最小单位。线程是一系列作业的组合。  
线程是独立调度和分派的基本单位。线程可以为操作系统内核调度的内核线程。  
线程的实体包含程序、数据和TCB。  
线程是动态概念，线程的动态特性有线程控制块TCB(Thread Control Block)描述。

一个TCB包含以下信息：

  * 线程状态
  * 线程中断现场
  * 执行堆栈
  * 线程局部变量主存
  * 进程的主存和其他资源

线程还有一些其他的信息也需要被保存：  
程序计数器，状态参数和返回地址等。  
![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/3616112f3273e49375e640d281cce45d.png)

### 1.3 进程

进程是程序在操作系统中的一次运行活动。  
进程是资源分配的基本单位。  
进程是操作系统结构的基础。  
进程是线程的容器。  
程序由指令，数据和组织形式构成。  
进程则是程序在运行环境下的一次运行活动。

狭义：进程是正在运行的程序的实例  
广义：进程是一个具有一定独立的程序的关于数据集合的一次运行活动。  
独立程序，表示程序其有静态的代码实现(二进制也是代码的一种体现)  
数据集合，运行时环境。

![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/0c0fde170e06500f5959571ec9d5a469.png)  
进程的动态特性是PCB实现的。

### 1.4 线程和进程的关系

前面有提到，进程可以看做是线程的容器。不管是进程还是线程都有自己的数据区，代码区以及动态控制器。  
线程可以访问线程所属的进程的资源，包含IO网络等资源。  
线程或者进程都有自己的状态，数据以及堆栈。  
线程依赖于进程。  
进程间通信IPC，线程之间通信可以直接读写数据(需进行并发处理)  
现代操作系统中，进程是资源调度的最小单位；线程是执行调度的最小单位。

因此，线程的上下文信息(状态，中断恢复，局部变量数据，程序调用堆栈等)远远小于进程的上下文信息。  
所以，线程的上下文切换快于进程的上下文切换。

### 1.5 协程

相比于进程，线程的上下文信息已经很小了，但是往往有些切换，有效操作的时间小于线程上下文切换的时间。也就是说，线程的上下文切换，还是浪费资源。  
在此基础上，提出了协程的概念，也叫微线程。  
协程简化了线程的上下文组成，只保留了程序调用堆栈和局部变量数据以及返回地址等。  
协程非常类似子程序调用过程。  
目前协程直接支持的编程语言不多。

### 1.6 协程和线程的关系

协程是基于线程的，所以协程切换不用切换线程上下文。也就是说，协程的调度中需要的资源是及其小的，几乎可以忽略不计。  
协程是线程自己调度的，可以根据业务进行调度。  
协程不需要线程并发的锁机制，不存在并发写读等冲突。

### 1.7 并行&串行

并行与串行：

串行：  
嘴巴和鼻子，在同一时刻，要么呼吸，要么喝水进食。  
你永远无法做到，同时呼吸，同时喝水进食。会被呛到。  
这就是串行，某一个时刻只能做一件事情。  
因为呼吸与喝水进食，并用咽喉。不管是进食，还是呼吸，都需要经过咽喉。一个人只有一个咽喉，所以，同一时间只能做一件事情。

并行：  
每个人都可以一边进食，一边听歌。  
因为进食与听歌使用的是不同的器官，他们之间没有冲突。所以可以在同一时刻，做多件事情。

在计算机的世界中，计算机进行操作的单位是处理器核心。如果处理器只有一个核心，那么就是串行的，如果处理器有多个核心，那么可以串行可以并行。

请注意：并行和串行研究的是时刻。

### 1.8 并发&并行

在过去的一个小时内，我吃了蔬菜，吃了米饭，喝了水。  
可以说，在这一个小时内，我并发吃了蔬菜，米饭以及喝水。  
所以，并发研究的是时间段，并行研究的是时间点。

### 1.9 阻塞&非阻塞

阻塞是指当线程或进程在运行状态，因请求资源，长时间未得到资源，被操作系统挂起，改变了状态，切换了上下文。  
非阻塞是指当线程或进程在运行状态，请求资源，在资源未得到响应期间，不会被操作系统挂起，不会修改状态，不会进行上下文切换。

### 1.10 同步&异步

同步：  
进程请求资源，在资源响应期间，进程没有做任何事情，在等待资源响应。

异步：  
进程请求资源，在资源响应期间，进程还做了其他的事情；资源响应后，继续执行请求资源后面的操作。

一般来说，同步是阻塞的，异步是非阻塞的。  
异步一般性能优于同步。

## 2\. 线程创建

### 2.1 线程组成

![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/fb7e6283a1a24138799b968a67d6c626.png)  
![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/54f51daa730db13456a5261c3fde8942.png)  
![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/ef7459d4ebf5a0921aaffd0b71639a0a.png)  
![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/e6a896072f71bd960d2781880ea50361.png)  
![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/d950fb9747f5d6132346e5adad472d69.png)  
![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/f7a26aa59ae59fd9b1fdd6fc45531f8a.png)  
线程状态

### 2.2 任务

线程可以驱动任务，任务就是线程需要执行的操作。  
任务可以由Runnable描述。
    
    
        static class LiftOff implements Runnable{
    
            protected int countDown = 10;
            private static int taskCount = 0;
            private final int id = taskCount++;
            public LiftOff(){}
            public LiftOff(int countDown){
                this.countDown = countDown;
            }
    
            public String status(){
                return "#" + id + "(" +
                        (countDown > 0 ? countDown : "LiftOff!") + ").";
            }
    
            @Override
            public void run() {
                while (countDown-- >=0 ){
                    System.out.println(status());
                    Thread.yield();
                }
            }
        }
    

这个任务是：打印当前的线程信息，以及线程内的计数器。  
每打印一次，就让出CPU执行权，等待下一次调度执行。
    
    
    public static void main(String[] args) throws InterruptedException {
            LiftOff liftOff = new LiftOff();
            liftOff.run();
    }
    

可以发现，自始至终，都是一个线程在运行。  
![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/36bd864b94443cf16f178f0b98848ab2.png)  
借助于调试，可以很清楚的明白，方法调用都是在main线程中。

### 2.3 线程驱动任务

我们在2.2中创建了任务，但是这个任务如果由主线程调用，那么就不是多线程了。而是方法调用。  
所以任务还需要进行线程驱动，如何进行线程驱动呢？
    
    
    public static void main(String[] args) throws InterruptedException {
            LiftOff liftOff = new LiftOff();
            new Thread(liftOff).start();
        }
    

![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/c7bfe737ec3e151567510f064af4bb93.png)  
这个时候，执行方法已经不是由main线程进行执行了，此时main线程已经结束了。  
如果到这里还不明显，那么，我们在主线程里面循环创建多个线程：
    
    
    public static void main(String[] args) throws InterruptedException {
            LiftOff liftOff = new LiftOff();
            for(int i = 0; i < 6;i++){
                new Thread(liftOff).start();
            }
        }
    

![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/6f010894cd57302476329f34d0fd8594.png)  
如果是串行执行呢？  
![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/16684fa022e17185ad2a81c1b70fd7d0.png)  
可以看到，串行是非常整齐的。但是如果是多线程，那么其执行的调入调出就不是可预测的了。

### 2.4 线程这样使用的问题

main线程创建了新的线程后，main线程没有捕获新创建的线程的任何引用。在使用普通对象时，这对于垃圾回收来说是一场公平的游戏，但是在使用Thread时，情况就不同了。每个Thread都注册了他自己：  
![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/31e9049365f041c4d5425dd0a1c23a86.png)  
因此，确实有一个对它的引用，而且，在它的任务退出run方法并死亡之前，垃圾回收器都无法清除。  
线程不仅仅包含任务，还有线程自己的一些线程信息，所以，线程创建与回收都是一件浪费资源的事情。  
那么，对于任务区别不大的任务，能不能用已有的线程驱动，避免线程的创建与回收？

## 3\. 线程池

### 3.1 Executor&ExecutorService

Executor是一个接口，定义了唯一的方法，参数就是任务。  
![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/daff9f77d7eced6a198604fd2e6f4f15.png)  
![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/ee18c338e7290198714e263080c3ccf1.png)  
![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/cc1cc213970333689f8952beb6cff9f1.png)  
ExecutorService定义了线程常用的统一的接口。

### 3.2 Executors

![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/f4e86e64e69c7916b4d9d3db91aaa407.png)  
![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/0d8638508f3c80f262544be8ee69e941.png)![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/a04dfc47bf600febfed51185aeee79cb.png)  
Executors的构造方法是私有的，也就是说，它不允许被实例化，他只是负责做一个代理工具。

### 3.1 newCachedThreadPool
    
    
        public static void main(String[] args) throws InterruptedException {
            ExecutorService service = Executors.newCachedThreadPool();
            for (int i = 0;i < 6;i++){
                service.execute(new LiftOff());
            }
            service.shutdown();
        }
    

![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/a2beb5dc59b62c9caa7e8a800fb6ea31.png)  
调用Executors的newCachedThreadPool的方法创建了一个线程池，初始0个线程，最多Integer.MAX_VALUE个线程。  
一个线程超时存活时间为60秒。  
如果线程的任务比较耗费资源，长时间无法释放线程，那么，在短时间创建大量的线程，很有可能出现OOM.  
阿里编码规范中强制不允许使用此种方式创建线程。  
调用了shutdown方法后，不在接受新的任务，而Executors线程将在所有子线程完成后尽快结束。

### 3.2 newFixedThreadPool
    
    
            ExecutorService service = Executors.newFixedThreadPool(6);
            for (int i = 0;i < 6;i++){
                service.execute(new LiftOff());
            }
            service.shutdown();
    

![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/cd4ce5ceb20c1363cbfd56b9c981c839.png)  
![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/dba4981d4bd34b122a91a1835f58e9bf.png)  
FixedTreadPool会在一开始就初始化指定的线程数量，在后面使用的时候，将会重复使用这些线程。  
不过，依然不推荐使用FixedTreadPool。因为FixedThreadPool允许创建的现场数量最多是Integer.MAX_VALUE，这很可能会引发OOM.

### 3.3 newScheduledThreadPool
    
    
            ExecutorService service = Executors.newScheduledThreadPool(3);
            for (int i = 0;i < 6;i++){
                service.execute(new LiftOff());
            }
            service.shutdown();
    

![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/8c710351c71f61d4025f3a9efda688eb.png)  
![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/352baee5b34f62749ee05e18090416f7.png)  
这和CachedThreadPool类似，最多可创建Integer.MAX_VALUE个线程，也是不推荐使用的。

### 3.4 newSingleThreadExecutor
    
    
            ExecutorService service = Executors.newSingleThreadExecutor();
            for (int i = 0;i < 6;i++){
                service.execute(new LiftOff());
            }
            service.shutdown();
    

这是线程池数量为1的线程池。  
![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/6ff92f1efe876c7bd556b63db375d741.png)  
这个线程池会初始化1一个线程，同时最多允许创建1个线程。  
![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/85a9d258389b73f33223ddc17508cd89.png)  
不过依然存在任务数量过多的问题。

### 3.5 ThreadPoolExecutor
    
    
            ExecutorService service = new ThreadPoolExecutor(3,5,
                    0L, TimeUnit.MILLISECONDS,new LinkedBlockingQueue<>(5));
            for(int i = 0;i<6;i++){
                service.execute(new LiftOff());
            }
            service.shutdown();
    

手动创建线程池，就可以根据业务创建需要的线程池。

## 4\. Callable

在2.2中的任务是没有返回值的，如果任务需要将计算结果返回，那么使用Runnable就难以实现有返回值的任务。  
基于此，Callable可以更好的定义有返回值的任务。
    
    
        static class RightOff implements Callable<String> {
    
            private int id;
    
            public RightOff(int id) {
                this.id = id;
            }
    
            @Override
            public String call() throws Exception {
                return "result of " + Thread.currentThread().getId() + ":" + id;
            }
        }
    
    
    
    ExecutorService service = new ThreadPoolExecutor(5, 10, 0L, TimeUnit.MILLISECONDS, new LinkedBlockingQueue<>());
            ArrayList<Future<String>> results = new ArrayList<>();
            for (int i = 0;i < 10;i++){
                results.add(service.submit(new RightOff(i)));
            }
            for(Future<String> future : results){
                try {
                    System.out.println(future.isDone());
                    System.out.println(future.get());
                } catch (ExecutionException e){
    				System.out.println("ExecutionException");
                    return;
                } catch (Exception e){
                    e.printStackTrace();
                    return;
                } finally {
                    service.shutdown();
                }
            }
    

submit方法会产生Future对象，它用Callable返回结果的特定类型进行参数化。  
可以调用isDone方法查询Future是否已经完成。  
当任务完成时，具有返回结果，可以调用get方法获取结果。  
也可以不用isDone方法判断是否完成，而是直接调用get方法获取结果。但是这种方法是阻塞的，程序main线程会一直阻塞在get方法，等待返回。为了防止长时间程序阻塞，可以调用带有超时时间的get方法。  
使用带有超时时间的get方法，如果在指定时间内，子线程没有交出结果，就会进行快速失败，抛出TimeOutException异常。

## 5\. 线程状态(生命周期)

通过查看Thread内部类，我们可以知道，线程总共有5个状态。  
![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/41730827263526e9f38776f5c7d36771.png)  
有些资料，将TIME_WAITING和WAITING放在了一起。  
在Java中则是分开的。  
![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/1318f6155c26a6b4fb0aeae1ae4b560c.png)  
其中WAITING和TIME_WAITING都是BLOCKED状态

### 5.1 NEW

当程序使用 new 关键字创建了一个线程之后，该线程就处于新建状态，此时仅由 JVM 为其分配  
内存，并初始化其成员变量的值。

### 5.2 RUNNABLE

当线程对象调用了 start()方法之后，该线程处于就绪状态。Java 虚拟机会为其创建方法调用栈和  
程序计数器，等待调度运行。

### 5.3 RUNNING

如果处于就绪状态的线程获得了 CPU，开始执行 run()方法的线程执行体，则该线程处于运行状  
态。

### 5.4 BLOCKED

阻塞状态是指线程因为某种原因放弃了 cpu 使用权，也即让出了 cpu timeslice，暂时停止运行。  
直到线程进入可运行(runnable)状态，才有机会再次获得 cpu timeslice 转到运行(running)状  
态。阻塞的情况分三种：

  * 等待阻塞：  
RUNNING状态执行了wait方法，JVM会把线程放入等待队列(waiting queue)中。
  * 同步阻塞：  
RUNNING状态的线程在获取对象的同步锁时，若该同步锁被别的线程占用，则JVM会把该线程放入锁池（Lock Pool）中。
  * 其他阻塞：  
RUNNINNGN状态的线程执行Thread.sleep或者Thread.join方法，或者发出资源请求时，JVM会把该线程设置为阻塞状态。当sleep状态超时，hoin等待线程终止或者超时，以及资源请求被响应时，线程重新进入RUNNABLE状态。

### 5.5 DEAD(TERMINATED)

线程死亡都是通过以下三种方式的。线程结束后，就是线程死亡状态了。

  * 正常结束：  
run或者call方法执行完成，线程正常结束。
  * 异常结束：  
线程抛出一个未捕获的Exception或者Error.
  * 调用stop：  
直接调用线程的stop方法结束线程(这种方式不安全，容易造成死锁)

## 6\. 终止线程的4种方式

### 6.1 正常运行结束

程序正常运行，直到结束。

### 6.2 使用退出标志退出线程

严格来说，使用退出标志退出线程，采用的也是正常运行结束的方式退出。
    
    
        static class Study implements Runnable{
    
            private volatile boolean exit = false;
            @Override
            public void run() {
                while (!exit){
                    // TODO: doSomething
                }
            }
        }
    
    

### 6.3 Interrupt中断线程

#### 6.3.1 线程阻塞

当线程本身处于阻塞状态时，调用线程的interrupt，抛出InterruptException异常。请注意，这个异常的抛出位置是阻塞的方法抛出的。线程捕获异常后，进行循环控制，实现正常退出线程。
    
    
        static class People extends Thread{
            @Override
            public void run() {
    
                while (true){
                    try {
                        System.out.println(Thread.currentThread().getName() + " sleep 1000 !");
                        Thread.sleep(1000);
                    }catch (InterruptedException e){
                        System.out.println("InterruptException");
                        break;
                    }
                }
                System.out.println(Thread.currentThread().getName()+"is down!");
    
            }
        }
    
    
    
            People people = new People();
            people.start();
            System.out.println("people is started!");
            System.out.println("main thread sleep 5000 !");
            try{
                Thread.sleep(5000);
            }catch (InterruptedException e){
                return;
            }
            people.interrupt();
            System.out.println("people interrupt!");
    

执行结果：  
![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/850d4e9c2046caad24b166253ad38dea.png)  
![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/fe4f3f7deb2a9d0e32f26405aa23518c.png)  
主要应该是调用了native方法。

#### 6.3.2 线程不阻塞

当线程不处于阻塞状态的时候，可以通过调用isInterrupt方法判断线程的中断标志退出线程。  
当调用interrupt方法时，中断标志被置位true.
    
    
        static class Goods extends Thread {
            @Override
            public void run() {
                int x = 0;
                System.out.println(getName() + " is in run method!");
                while (!isInterrupted()) {
                    System.out.println(getName() + " input while " + x);
                    x++;
                }
                System.out.println(getName() + " is out run method!");
            }
        }
    
    
    
            System.out.println(Thread.currentThread().getName() + " is start ");
            Goods goods = new Goods();
            System.out.println(Thread.currentThread().getName() + " goods is new ");
            goods.start();
            System.out.println(Thread.currentThread().getName() + " goods is running");
            System.out.println(Thread.currentThread().getName() + " sleep 3 ");
            Thread.sleep(3);
            goods.interrupt();
    

执行结果：  
![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/91c16e341598ea9d40ca26491e890a66.png)  
![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/142b827850cea70e1dc3859bc5a8c236.png)

### 6.4 stop方法终止线程

程序中可以直接使用 thread.stop()来强行终止线程，但是 stop 方法是很危险的，就象突然关  
闭计算机电源，而不是按正常程序关机一样，可能会产生不可预料的结果，不安全主要是：  
thread.stop()调用之后，创建子线程的线程就会抛出 ThreadDeatherror 的错误，并且会释放子  
线程所持有的所有锁。一般任何进行加锁的代码块，都是为了保护数据的一致性，如果在调用  
thread.stop()后导致了该线程所持有的所有锁的突然释放(不可控制)，那么被保护数据就有可能呈  
现不一致性，其他线程在使用这些被破坏的数据时，有可能导致一些很奇怪的应用程序错误。因  
此，并不推荐使用 stop 方法来终止线程。

## 7\. sleep,wait,yield,join

>   * sleep  
>  sleep()方法需要指定等待的时间，它可以让当前正在执行的线程在指定的时间内暂停执行，进入阻塞状态，该方法既可以让其他同优先级或者高优先级的线程得到执行的机会，也可以让低优先级的线程得到执行机会。但是sleep()方法不会释放“锁标志”，也就是说如果有synchronized同步块，其他线程仍然不能访问共享数据。
>   * wait  
>  wait()方法需要和notify()及notifyAll()两个方法一起介绍，这三个方法用于协调多个线程对共享数据的存取，所以必须在synchronized语句块内使用，也就是说，调用wait()，notify()和notifyAll()的任务在调用这些方法前必须拥有对象的锁。注意，它们都是Object类的方法，而不是Thread类的方法。  
>  wait()方法与sleep()方法的不同之处在于，wait()方法会释放对象的“锁标志”。当调用某一对象的wait()方法后，会使当前线程暂停执行，并将当前线程放入对象等待池中，直到调用了notify()方法后，将从对象等待池中移出任意一个线程并放入锁标志等待池中，只有锁标志等待池中的线程可以获取锁标志，它们随时准备争夺锁的拥有权。当调用了某个对象的notifyAll()方法，会将对象等待池中的所有线程都移动到该对象的锁标志等待池。  
>  除了使用notify()和notifyAll()方法，还可以使用带毫秒参数的wait(long timeout)方法，效果是在延迟timeout毫秒后，被暂停的线程将被恢复到锁标志等待池。
>   * yield  
>  yield()方法和sleep()方法类似，也不会释放“锁标志”，区别在于，它没有参数，即yield()方法只是使当前线程重新回到可执行状态，所以执行yield()的线程有可能在进入到可执行状态后马上又被执行，另外yield()方法只能使同优先级或者高优先级的线程得到执行机会，这也和sleep()方法不同。
>   * join  
>  join()方法会使当前线程等待调用join()方法的线程结束后才能继续执行。
> 

<https://blog.csdn.net/xiangwanpeng/article/details/54972952>

## 8\. start与run

  * start  
用start方法来启动线程，真正实现了多线程运行，这时无需等待run方法体代码执行完毕而直接继续执行下面的代码。通过调用Thread类的start()方法来启动一个线程，这时此线程处于就绪（可运行）状态，并没有运行，一旦得到cpu时间片，就开始执行run()方法，这里方法 run()称为线程体，它包含了要执行的这个线程的内容，Run方法运行结束，此线程随即终止。
  * run  
run()方法只是类的一个普通方法而已，如果直接调用Run方法，程序中依然只有主线程这一个线程，其程序执行路径还是只有一条，还是要顺序执行，还是要等待run方法体执行完毕后才可继续执行下面的代码，这样就没有达到写线程的目的。

**总结：调用start方法方可启动线程，而run方法只是thread的一个普通方法调用，还是在主线程里执行。这两个方法应该都比较熟悉，把需要并行处理的代码放在run()方法中，start()方法启动线程将自动调用 run()方法，这是由jvm的内存机制规定的。并且run()方法必须是public访问权限，返回值类型为void。**

## 9\. daemon

### 9.1 什么是daemon进程

指在程序运行的时候在后台提供一种通用服务的线程，并且这种线程并不属于程序中不可或缺的部分。  
当程序中全部是后台进程时，后台进程将自动终止，同时系统也结束了。

### 9.2 优先级

daemon进程的优先级比较低，用于为系统中的其他对象和线程提供服务。
    
    
            Runnable daemonDemo = () -> {
                try{
                    TimeUnit.SECONDS.sleep(2);
                } catch (InterruptedException e){
                    System.out.println("daemon demo interrupt exception");
                } finally {
                    System.out.println("deamon demo finally");
                }
            };
            Thread daemon = new Thread(daemonDemo);
            daemon.setDaemon(true);
            daemon.setPriority(Thread.MAX_PRIORITY);
            daemon.start();
            System.out.println("daemon priority is " + daemon.getPriority());
            System.out.println("main priority is " + Thread.currentThread().getPriority());
    

![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/f433effdb91dabddd6a7efeafcd1889e.png)  
JDK中有10个优先级，但是JDK与多数操作系统都不能映射很好。  
Windows有7个优先级，且不是固定的，所以映射关系也不是确定的。  
sun的solaris有2^31个优先级。

所以，调整优先级的时候，尽可能使用MAX_PRIORITY,NORM_PRIORITY,MIN_PRIORITY三种级别。

### 9.3 daemon创建

一个线程在创建之后，启动之前，调用setDaemon方法设置是否是daemon线程  
![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/425670e5ba9ef52461681da89e10622d.png)  
如果在启动之后调用，则会抛出异常。

### 9.4 daemon传递性

daemon线程创建的线程也是daemon线程。
    
    
            Runnable daemonDemo = () ->{
                System.out.println("daemon by main start");
                Runnable daemon = () -> {
                    System.out.println("daemon by daemon start");
                    try {
                        TimeUnit.SECONDS.sleep(1);
                    } catch (InterruptedException e){
                        System.out.println("daemon by daemon interrupt exception");
                    } finally {
                        System.out.println("daemon by daemon finally");
                    }
                };
                System.out.println("daemon by main create thread");
                Thread daemonByDaemon = new Thread(daemon);
                System.out.println("daemon by daemon get isDaemon : " + daemonByDaemon.isDaemon());
                daemonByDaemon.start();
            };
            Thread daemonDemoThread = new Thread(daemonDemo);
            daemonDemoThread.setDaemon(true);
            System.out.println("daemon by main get isDaemon : " + daemonDemoThread.isDaemon());
            daemonDemoThread.start();
            TimeUnit.SECONDS.sleep(2);
    

![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/04c1383814af7080acab9ba5f2d8d4d1.png)

### 9.5 daemon生命周期

daemon运行在后台的一种特殊进程。它独立于控制终端并且周期性地执行某种任务或等待处理某些发生的事件。也就是说守护线程不依赖于终端，但是依赖于系统，与系统“同生共死”。当 JVM 中所有的线程都是守护线程的时候，JVM 就可以退出了；如果还有一个或以上的非守护线程则 JVM 不会退出。  
基于此性质，可能存在这样的情况：**finally块可能不会被执行**
    
    
            Runnable daemonDemo = () -> {
                try{
                    System.out.println("daemon demo is starting run !");
                    TimeUnit.SECONDS.sleep(1);
                } catch (InterruptedException e){
                    System.out.println("interrupt exception !");
                } finally {
                    System.out.println("finally daemon demo !");
                }
            };
            System.out.println("c create daemon thread");
            Thread daemonThread = new Thread(daemonDemo);
            System.out.println("main thread set daemon is true");
            daemonThread.setDaemon(true);
            System.out.println("main thread start");
            daemonThread.start();
            System.out.println("main is over");
            TimeUnit.MILLISECONDS.sleep(300);
    

守护线程，sleep1秒后，应该执行finally语句。但是因为主线程只sleep300毫秒，300毫秒后，主线程结束，此时守护线程也就结束了。  
![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/6daa8f3b10c2ace07d0710a54cb689bc.png)

## 10\. 自管理Runnable

实现线程可以继承Thread类。但是有时候，我们还需要继承其他的类的时候，就只能实现Runnable接口，实现了接口，则需要调用者去驱动任务。  
当有时候，我们需要将驱动工作也封装，就需要使用自管理的Runnable。  
自管理的Runnable也很简单，举例说明：  
假设有类A，封装了一些通用操作。类B需要继承类A，然后实现Runablle接口。  
为了实现自管理，需要在类B中增加属性Thread，通过传入类B自身，进行创建自身的Thread，然后提供获取本身Thread的方法，进行处理。
    
    
        static class Father {
            public void say() {
                System.out.println("father say!");
            }
        }
    
        static class Child extends Father implements Runnable {
    
            private Thread thread = new Thread(this);
    
            {
                thread.start();
            }
    
            public Thread getThread(){
                return thread;
            }
    
            @Override
            public void run() {
                try {
                    while(true){
                        System.out.println("child run!");
                        TimeUnit.SECONDS.sleep(1);
                    }
                } catch (InterruptedException e) {
                    System.out.println("child interrupt exception!");
                } finally {
                    System.out.println("child finally!");
                }
            }
        }
    
    
    
    
            Child child = new Child();
            child.say();
            TimeUnit.SECONDS.sleep(3);
            System.out.println(child.getThread().isAlive());
            child.getThread().interrupt();
    

运行结果：  
![在这里插入图片描述](https://picgo-1302191088.cos.ap-guangzhou.myqcloud.com/csdn/csdnimg/4f1a17379c87563cb186496aaf5b383b.png)
